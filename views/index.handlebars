<div class="card">
    <div id="main-title" class="text-center card-img-top">
        <h1 class="text-center align-middle">Grocery Shopping List</h1>
    </div>
    <div class="card-body">
        <h5 class="card-title text-center">Instructions</h5>
        <p class="card-text">Create a grocery shopping list and assign the item to someone in your hosehold. You can update your records when
            the item has been purchased</p>
        <form action="/wish" method="POST">
            <div class="form-group">
                <label for="item">Item name</label>
                <input name="item" type="text" class="form-control" id="item" placeholder="I'm running out of...">
            </div>
            <div class="form-group">
                <label for="person">Person</label>
                <input name="person" type="text" class="form-control" id="person" placeholder="Who's buying it...">
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-lg btn-danger">Send</button>
            </div>
        </form>
    </div>
</div>
<br />
<div class="list-group">
    <a href="#" class="text-center list-group-item list-group-item-action active bg-danger">
        Pending
    </a>
    {{#each itemPending}}
        <a href="#" class="list-group-item list-group-item-action"><strong>Id:</strong> {{this.id}} || <strong>Product:</strong> {{this.item}} || <strong>Who:</strong> {{this.person}}</a>
    {{/each}}
</div>
<br /> 
<div class="list-group">
    <a href="#" class="text-center list-group-item list-group-item-action active bg-info">
        Completed
    </a>
    {{#each itemComplete}}
        <a href="#" class="list-group-item list-group-item-action"><strong>Id:</strong> {{this.id}} || <strong>Product:</strong> {{this.item}} || <strong>Who:</strong> {{this.person}}</a>
    {{/each}}
</div>
<br />
<br />
